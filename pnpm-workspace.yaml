packages:
  # anton-business/apps/anton-business-app moved to _archive/expo-attempt/
  # on 2026-05-14 after the Capacitor pivot. See that dir's README for the
  # full story. The futurechain-sdk package below stays — src/business/
  # imports it as @futurechain/sdk.
  - 'anton-business/packages/*'
  # Desktop apps (Anton Agent Pay etc) live under apps/. Each app has
  # its own package.json and may consume @futurechain/sdk via workspace:*.
  - 'apps/*'
